todo:

  core:
    - finish ripping pipeline
        - write statistics
        - write iso writer
            - add splitted iso image support
        - add proper file writing & file naming
        - add asynchronous processing loops and thread
        - add title (DITI) & artist (DIAR) to DSDIFF
        - test the incorrect output length (of by a couple of frames)
    
    - write log along each consecutive rip

  interface: 
    - ask which area to rip (in case of multiple)
    - show not enough disc space message (do fstat during ripping)

- add dst decoding
- add sony dsf export support
